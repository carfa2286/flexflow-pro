{
  "name": "loan-management-app",
  "version": "1.0.0",
  "description": "Sistema de gestión de préstamos con recordatorios automáticos y reportes visuales",
  "main": "server.js",
  "scripts": {
    "start": "npm run build && node server-prod.js",
    "build": "cd client && npm install && npm run build",
    "dev": "nodemon server.js",
    "client": "cd client && npm start"
  },
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.18.2",
    "node-cron": "^3.0.2",
    "nodemailer": "^6.9.1",
    "pg": "^8.22.0",
    "sqlite3": "^5.1.6",
    "twilio": "^6.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
