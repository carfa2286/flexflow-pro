services:
  - type: web
    name: flexflow-pro
    env: node
    buildCommand: "cd client && npm install && CI=false npm run build && cd .. && npm install"
    startCommand: "node server.js"
    envVars:
      - key: NODE_ENV
        value: production
